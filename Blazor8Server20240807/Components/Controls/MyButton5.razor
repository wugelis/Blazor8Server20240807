<button class="btn btn-primary" @onclick="ChangeCount">@ChildContent @count</button>

@code {
    int count = 0;
    [Parameter] public RenderFragment ChildContent { get; set; }

    async Task ChangeCount()
    {
        count++;
    }
}
